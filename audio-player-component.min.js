(() => {
  // audio-player-component.ts
  var m = {
    aac: "audio/aac",
    caf: "audio/caf; codecs=opus",
    mp3: "audio/mp3",
    mp4: "audio/mp4",
    m4a: "audio/mp4",
    oga: "audio/ogg; codecs=vorbis",
    ogg: "audio/ogg; codecs=vorbis",
    opus: "audio/ogg; codecs=opus",
    wav: "audio/wave",
    webm: "audio/webm"
  }, c = class t extends HTMLElement {
    #s;
    #e;
    #a;
    #n;
    #t = document.createElement("audio");
    #i = null;
    #o = !1;
    #l = !1;
    #r;
    static get observedAttributes() {
      return [
        "title",
        "artist",
        "image",
        "compact",
        "blurred",
        "min-height",
        "sources",
        "colour",
        "dark",
        "length-secs"
      ];
    }
    static get svgPlay() {
      return '<svg xmlns="https://www.w3.org/2000/svg" width="24" height="24" viewBox="-1 0 23 24"><title>Play</title><polygon class="icon-play" points="19.05 12 6 3.36 6 20.64 19.05 12"/></svg>';
    }
    static get svgPause() {
      return '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24"><title>Pause</title><g><rect class="icon-pause" x="6" y="3" width="4" height="18"/><rect class="icon-pause" x="14" y="3" width="4" height="18"/></g></svg>';
    }
    static get svgLoading() {
      return '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" class="loading"><circle cx="12" cy="4" r="2.5"/><circle cx="6" cy="6" r="2.25"/><circle cx="4" cy="12" r="2"/><circle cx="6" cy="18" r="1.75"/><circle cx="12" cy="20" r="1.5"/><circle cx="18" cy="18" r="1.25"/><circle cx="20" cy="12" r="1"/><circle cx="18" cy="6" r="0.75"/></svg>';
    }
    static get svgPlus() {
      return '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24"><title>Add to Queue</title><polygon points="21,10 14,10 14,3 10,3 10,10 3,10 3,14 10,14 10,21 14,21 14,14 21,14 "/></svg>';
    }
    constructor() {
      super(), this.#r = this.attachShadow({ mode: "open" }), this.#s = this.getAttribute("title"), this.#e = this.getAttribute("artist"), this.#a = this.getAttribute("album"), this.#n = this.getAttribute("compact") !== null;
      let e = this.getAttribute("preload") ?? "auto";
      (e === "" || e === "none" || e === "auto" || e === "metadata") && (this.#t.preload = e);
      let a = this.getAttribute("sources")?.split(",") ?? [];
      this.loadAudio(a);
    }
    static padTime(e) {
      return e < 10 ? `0${e}` : e.toString();
    }
    static createElement({ tagName: e = "div", className: a = "", ...s } = {}) {
      let o = document.createElement(e);
      o.className = a;
      for (let [l, r] of Object.entries(s))
        o[l] = r;
      return o;
    }
    loadAudio(e) {
      if (e.length === 1) {
        this.#t.src = e[0];
        return;
      }
      for (let a of e) {
        let s = a.split(".").pop(), o = m[s ?? "mp3"];
        if (!o) {
          console.warn(`Could not find MIME-type for '${s}' type. Skipping.`);
          continue;
        }
        this.#t.appendChild(
          t.createElement({
            tagName: "source",
            src: a,
            type: o
          })
        );
      }
    }
    get styleCss() {
      return `
		.tap--container {
			width: 100%;
			position: relative;
			display: flex;
			flex-direction: column;
			overflow: hidden;
			border-radius: 32px;
			box-shadow: 0 2px 16px rgba(0, 0, 0, 0.25);
			justify-content: flex-end;
		}

		.tap--overlay {
			background: linear-gradient(rgba(0, 0, 0, 0) 0%, rgba(0, 0, 0, 0.5) 100%);
			position: absolute;
			width: 100%;
			height: 100%;
			z-index: -998;
		}

		.tap--img {
			position: absolute;
			width: 100%;
			height: 100%;
			object-fit: cover;
			z-index: -999;
		}

		.tap--img.blurred {
			filter: blur(16px);
		}

		.tap--metadata {
			display: flex;
			flex-direction: column;
			margin: 32px;
			color: #fff;
			user-select: none;
			text-shadow: 0 2px 8px rgba(0, 0, 0, 0.4);
		}

		.tap--metadata__title {
			font-size: 32px;
			font-weight: 700;
			display: block;
			margin-bottom: 16px;
		}

		.tap--metadata__artist {
			font-size: 28px;
			font-weight: 400;
		}

		.tap--container .tap--controls {
			margin: 0 16px 16px;
		}

		.tap--controls {
			display: flex;
			background-color: var(--audio-player-controls-bg);
			border-radius: 32px;
			height: 64px;
			align-items: center;
			box-shadow: 0 2px 16px rgba(0, 0, 0, 0.25);

			--audio-player-controls-bg: #f9f9f9;
			--audio-player-button: #fff;
			--audio-player-button-hover: #f9f9f9;
			--audio-player-border: #999;
			--audio-player-border-hover: #666;
			--audio-player-progress: #f0f0f0;
			--audio-player-text: #333;
		}
		.tap--controls.dark {
			--audio-player-controls-bg: #222;
			--audio-player-button: #333;
			--audio-player-button-hover: #555;
			--audio-player-border: #aaa;
			--audio-player-border-hover: #bbb;
			--audio-player-progress: #555;
			--audio-player-text: #fff;
		}

		.tap--button {
			cursor: pointer;
			font-size: 24px;
			border-radius: 24px;
			width: 48px;
			height: 48px;
			position: relative;
			margin: 0 8px;
			background-color: var(--audio-player-button);
		}

		.tap--button:hover {
			border: 2px solid var(--audio-player-border-hover);
			background-color: var(--audio-player-button-hover);
		}

		.tap--button,
		.tap--progress--bar {
			border: 2px solid var(--audio-player-border);
		}

		.tap--button svg {
			position: absolute;
			top: 12.5%;
			left: 12.5%;
			width: 75%;
			height: 75%;
			fill: var(--audio-player-text);
		}

		.tap--progress--bar {
			flex: 1;
			height: 24px;
			background-color: var(--audio-player-progress);
			border-radius: 12px;
			margin: 0;
			cursor: pointer;
			position: relative;
			overflow: hidden;
		}

		.tap--progress--playhead {
			border-radius: 0 12px 12px 0;
			position: absolute;
			top: 0;
			left: 0;
			height: 100%;
			width: 0%;
			pointer-events: none;
			transition: width 0.2s;
		}

		.tap--progress--playhead {
			box-shadow: 0 0 32px rgba(0, 0, 0, 0.75);
		}

		.tap--progress--timestamp {
			font-weight: 700;
			font-family: monospace;
			margin: 0 8px;
			color: var(--audio-player-text);
			user-select: none;
		}

		.loading {
			animation: 1s infinite loading linear;
		}

		@keyframes loading {
			from { transform: rotate(0); }
			to { transform: rotate(360deg); }
		}

		@media (max-width: 640px) {
			.tap--metadata__title {
				font-size: 24px;
			}
			.tap--metadata__artist {
				font-size: 18px;
			}
			.tap--container .tap--controls {
				margin: 8px;
			}
		}
		`;
    }
    addOverlayToContainer(e) {
      if (this.#n) return;
      let a = t.createElement({
        tagName: "img",
        className: "tap--img",
        src: this.getAttribute("image") || ""
      }), s = this.getAttribute("blurred");
      if ((s === null ? !!(this.#s && this.#e && this.#a) : s !== "false") && a.classList.add("blurred"), e.appendChild(a), !(this.#s || this.#e || this.#a)) return;
      let l = t.createElement({ className: "tap--overlay" }), r = t.createElement({ className: "tap--metadata" }), p = t.createElement({
        tagName: "span",
        className: "tap--metadata__title",
        textContent: this.#s
      }), n = "";
      this.#e === null && this.#a !== null ? n = this.#a : this.#e !== null && this.#a === null ? n = this.#e : this.#e !== null && this.#a !== null && (n = `from ${this.#a}, by ${this.#e}`);
      let i = t.createElement({
        tagName: "span",
        className: "tap--metadata__artist",
        textContent: n
      });
      r.appendChild(p), r.appendChild(i), e.appendChild(l), e.appendChild(r);
    }
    connectedCallback() {
      this.#r.innerHTML = "", this.#r.appendChild(t.createElement({ tagName: "style", textContent: this.styleCss }));
      let e = t.createElement({ className: "tap--container" });
      if (!this.#n) {
        let i = Number(this.getAttribute("min-height") ?? 280), d = Number.isSafeInteger(i) ? i : 280;
        e.style.minHeight = `${d}px`;
      }
      this.addOverlayToContainer(e);
      let a = t.createElement({ className: "tap--controls" });
      this.getAttribute("dark") !== null && a.classList.add("dark");
      let s = t.createElement({ className: "tap--progress--bar" }), o = t.createElement({ className: "tap--progress--playhead" }), l = t.createElement({ className: "tap--progress--timestamp" });
      this.#i = t.createElement({
        tagName: "button",
        className: "tap--button",
        innerHTML: t.svgPlay
      }), o.style.backgroundColor = this.getAttribute("colour") || this.getAttribute("color") || "#3FA9F5";
      let r = () => {
        let i = Number(this.getAttribute("length-secs") ?? 0), d = t.padTime(Math.floor(this.#t.currentTime / 60)), h = t.padTime(Math.floor(this.#t.currentTime % 60)), u = t.padTime(Math.floor((this.#t.duration || i) / 60)), g = t.padTime(Math.floor((this.#t.duration || i) % 60));
        l.innerHTML = `${d}:${h}<br/>${u}:${g}`, o.style.width = `${this.#t.currentTime * 100 / (this.#t.duration || 1)}%`;
      };
      this.#i.onclick = () => {
        if (window.TomboAudioPlayer?.addToPlaylistAndPlay) {
          window.TomboAudioPlayer.addToPlaylistAndPlay({
            title: this.#s,
            artist: this.#e,
            album: this.#a,
            src: this.#t.src,
            imageSrc: this.getAttribute("image")
          });
          return;
        }
        if (!(this.#o && !this.#l)) {
          if (!this.#o) return this.play();
          this.pause();
        }
      };
      let p = (i) => {
        let d = i.layerX / i.target.offsetWidth;
        this.#t.fastSeek(this.#t.duration * d);
      }, n = !1;
      if (s.onmousedown = () => {
        n = !0;
      }, s.onmouseup = () => {
        n = !1;
      }, s.onmousemove = (i) => n && p(i), s.onclick = p, s.appendChild(o), a.appendChild(this.#i), a.appendChild(s), a.appendChild(l), window.TomboAudioPlayer?.addToPlaylist) {
        let i = t.createElement({
          tagName: "button",
          className: "tap--button",
          innerHTML: t.svgPlus
        });
        i.onclick = () => {
          window.TomboAudioPlayer.addToPlaylist({
            title: this.#s,
            album: this.#a,
            artist: this.#e,
            src: this.#t.src,
            imageSrc: this.getAttribute("image")
          });
        }, a.appendChild(i);
      }
      if (r(), this.#t.ondurationchange = r, this.#t.ontimeupdate = r, this.#t.onended = () => {
        this.#t.currentTime = 0, this.#o = !1, this.#i.innerHTML = t.svgPlay;
      }, this.#t.style.display = "none", this.#n) {
        this.#r.appendChild(a);
        return;
      }
      e.appendChild(a), this.#r.appendChild(e);
    }
    async play() {
      this.#o = !0, this.#t.readyState <= 1 && (this.#t.load(), this.#i.innerHTML = t.svgLoading), await this.#t.play(), this.#l = !0, this.#i.innerHTML = t.svgPause;
    }
    pause() {
      this.#t.pause(), this.#o = !1, this.#i.innerHTML = t.svgPlay;
    }
    stop() {
      this.pause(), this.#t.fastSeek(0);
    }
  };
  window.customElements.define("audio-player", c);
})();
